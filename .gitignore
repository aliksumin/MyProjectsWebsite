# Original source material — kept on disk, deliberately not published.
# Contains the raw uploads (CV, interview documents, PDFs, camera originals)
# and archived page snapshots. ~3.3 GB, far beyond what a GitHub repo should hold.
_source/

# The design-project export this site was built from.
MyWebPage.zip

# Build-tool dependencies. The site itself needs none of this.
tools/node_modules/

# Local editor/agent config — contains machine-specific absolute paths
.claude/

# Migration log
tools/migrate-report.txt

# OS / editor noise
Thumbs.db
Desktop.ini
.DS_Store
*~
